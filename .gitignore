# build output
dist/
# dependencies
node_modules/
# generated types
.astro/
# macOS
.DS_Store
# env
.env
.env.production
# logs
npm-debug.log*
# design prototypes — local reference only, ~54MB of HTML exports and screenshots.
# The live site is built from src/; these are the Figma-side handoff artifacts.
# Delete a line here if you want that folder tracked in the repo instead.
design-reference/
design-reference-v08/
ybor-website-v-01.zip
